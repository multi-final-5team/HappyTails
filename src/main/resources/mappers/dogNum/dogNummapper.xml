<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.multi.happytails.dogNum.dao.DogNumDAO">

    <!-- INSERT 쿼리 정의 -->
    <insert id="insert" parameterType="com.multi.happytails.dogNum.dto.DogNumDTO">
        INSERT INTO TB_DOGNUM (DOGNUM, DOGNAME, DOGGENDER, BREED, NEUTERING)
        VALUES (#{dognum}, #{dogname}, #{doggender}, #{breed}, #{neutering})
    </insert>


    <select id="getCurrentDogNumNo">
        SELECT DOGNUM_NO
        FROM TB_DOGNUM
        ORDER BY DOGNUM_NO DESC
        LIMIT 1
    </select>
</mapper>
