<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.multi.happytails.upload.model.dao.UploadMapper">

    <insert id="uploadInsert" useGeneratedKeys="true" keyProperty="imageNo">
        INSERT INTO TB_FILE (IMAGE_NO, IMAGE_NAME, STORED_FILE_NAME, FOREIGN_TYPE, FOREIGN_NO)
        VALUES(#{imageNo}, #{imageName}, #{storedFileName}, #{foreignType}, #{foreignNo})
    </insert>

    <select id="uploadSelectList">
        SELECT STORED_FILE_NAME FROM TB_FILE WHERE FOREIGN_TYPE = #{foreignType} AND FOREIGN_NO = #{foreignNo} ORDER BY FILE_NO ASC
    </select>

</mapper>
